# Agent Rules

- Do not create pull requests unless the user explicitly asks for one.
- Do not create new branches unless the user explicitly asks for one.
- When asked to submit to GitHub, commit on the current branch and push the current branch to its existing upstream.
